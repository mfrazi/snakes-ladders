<?xml version="1.0" encoding="UTF-8"?>
<!--
  A single-page game, so this has exactly one entry. It exists because the
  Sitemap line in robots.txt points at it, and a 404 there is a crawl error
  rather than a neutral absence.

  The origin is a placeholder that worker.js rewrites to the real host on the
  way out — it must match PLACEHOLDER_ORIGIN there and the canonical tag in
  index.html, since the rewrite is a literal string match.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://snake-ladder-love-edition.workers.dev/</loc>
    <lastmod>2026-09-14</lastmod>
    <changefreq>monthly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>

{
  "name": "snakes-ladders",
  "private": true,
  "version": "1.0.0",
  "description": "A Snakes & Ladders game for couples or friends where every square starts a conversation — plain HTML/CSS/JS, deployable as a static site.",
  "scripts": {
    "start": "python3 serve.py",
    "dev": "npm run build && wrangler dev",
    "build": "node scripts/build.js",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "terser": "^5.51.2",
    "wrangler": "latest"
  }
}

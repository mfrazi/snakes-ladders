{
  "name": "ladder-snake",
  "private": true,
  "version": "1.0.0",
  "description": "A Snake & Ladder game for couples or friends where every square starts a conversation — plain HTML/CSS/JS, deployable as a static site.",
  "scripts": {
    "start": "python3 serve.py",
    "dev": "wrangler dev",
    "build": "node scripts/build.js",
    "deploy": "npm run build && wrangler deploy --assets=dist"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "terser": "^5.51.2",
    "wrangler": "4.131.2"
  }
}
